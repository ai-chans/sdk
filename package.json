{
  "name": "chans-sdk-js",
  "private": true,
  "description": "Official JavaScript SDK for chans.ai voice AI",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "clean": "rm -rf node_modules client/node_modules react/node_modules client/dist react/dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=18"
  }
}
